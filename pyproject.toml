[project]
name = "fake-news-detector"
version = "0.1.0"
description = "Fake News Detector"

dependencies = [
    "fastapi",
    "uvicorn",
    "transformers",
    "torch",
    "accelerate",
    "openenv>=0.2.0"
]

[project.scripts]
server = "server.app:app"