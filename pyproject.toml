[project]
name = "fake-news-detector"
version = "0.1.0"

dependencies = [
    "fastapi",
    "uvicorn",
    "transformers",
    "torch",
    "accelerate",
    "openenv-core"
]

[project.scripts]
server = "server.app:app"