[project]
name = "fake-news-detector"
version = "0.1.0"

dependencies = [
    "fastapi",
    "uvicorn",
    "transformers",
    "torch",
    "accelerate",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"